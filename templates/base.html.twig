<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DA BOIT Maxence - Portfolio{% endblock %}</title>
    
    {# Bootswatch Theme : LUX #}
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.2/dist/lux/bootstrap.min.css" rel="stylesheet">

    {# Polices et Icônes #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body { font-family: 'Inter', sans-serif; }
        a { text-decoration: none !important; }
        
        /* Tech Stack Scrolling */
        .tech-stack-wrapper { overflow: hidden; padding: 40px 0; }
        .tech-stack { display: flex; gap: 50px; width: max-content; }
        .tech-item { flex-shrink: 0; }
        
        .project-image { height: 280px; }
        .project-image.gradient-1 { background: linear-gradient(135deg, #ffeaa7 0%, #fab1a0 50%, #e17055 100%); }
        .project-image.gradient-2 { background: linear-gradient(135deg, #a29bfe 0%, #74b9ff 50%, #81ecec 100%); }
        .project-image.gradient-3 { background: linear-gradient(135deg, #fd79a8 0%, #fdcb6e 50%, #e17055 100%); }
        
        .interest-card { height: 200px; border-radius: 12px; }
        .interest-card.gradient-1 { background: linear-gradient(135deg, #ffeaa7 0%, #fab1a0 100%); }
        .interest-card.gradient-2 { background: linear-gradient(135deg, #a29bfe 0%, #74b9ff 100%); }
        .interest-card.gradient-3 { background: linear-gradient(135deg, #dfe6e9 0%, #b2bec3 100%); }
        
        .university-logo { width: 240px; height: 160px; margin: 0 auto; }
        .university-logo img { width: 100%; height: 100%; object-fit: contain; }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {# Navigation #}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">DA BOIT Maxence</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_cv') }}">CV</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_eportfolio') }}">E-Portfolio</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_passions') }}">Mes Passions</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_contact') }}">Formulaire</a></li>
                </ul>
            </div>
        </div>
    </nav>

    {# Main Content #}
    <main>
        {% block body %}{% endblock %}
    </main>

    {# Footer avec protection copyright - OBLIGATOIRE cahier des charges #}
    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5 class="fw-bold">DA BOIT Maxence</h5>
                    <p class="mb-2">Étudiant en BUT R&T - IUT de Roanne</p>
                    <p class="small mb-0">Groupe TP: {{ groupe_tp|default('B1') }}</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-2">
                        <a href="#" class="text-white me-3"><i class="fab fa-linkedin"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-github"></i></a>
                        <a href="mailto:contact@daboit.fr" class="text-white"><i class="fas fa-envelope"></i></a>
                    </p>
                    <p class="small mb-1">© {{ "now"|date("Y") }} DA BOIT Maxence. Tous droits réservés.</p>
                    <p class="small mb-0">Ce site et son contenu sont protégés par le droit d'auteur.</p>
                    <p class="small">Toute reproduction interdite sans autorisation.</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>