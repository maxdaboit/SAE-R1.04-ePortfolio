<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DA BOIT - Portfolio{% endblock %}</title>
    
    {# Bootswatch #}
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.2/dist/flatly/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        :root {
            --accent-color: #5227FF;
            --text-primary: #000;
            --text-secondary: #666;
            --bg-color: #ffffff; /* Fond blanc standard */
        }
        
        /* --- LAYOUT GÉNÉRAL --- */
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            overflow-x: hidden;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        main {
            flex: 1; /* Pour que le footer reste en bas si peu de contenu */
        }

        a { text-decoration: none !important; }

        /* --- HEADER (Style Verre Moderne) --- */
        .header {
            padding: 1rem 0;
            border-bottom: 1px solid rgba(0,0,0,0.05);
            background: rgba(255, 255, 255, 0.85); /* Semi-transparent */
            backdrop-filter: blur(10px); /* Effet de flou sur le contenu qui passe dessous */
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .container-custom { max-width: 1200px; margin: 0 auto; padding: 0 2rem; }
        .header-content { display: flex; justify-content: space-between; align-items: center; }
        .logo-section h1 { font-size: 1.1rem; font-weight: 600; margin: 0; }
        .logo-section p { color: var(--text-secondary); font-size: 0.95rem; margin: 0; }

        /* --- MENU --- */
        .menu-toggle { background: none; border: none; cursor: pointer; display: flex; align-items: center; gap: 12px; font-weight: 600; z-index: 101; color: #000; }
        .menu-text-wrap { height: 1.2em; overflow: hidden; text-align: right; font-size: 0.9rem; }
        .menu-text-inner { display: flex; flex-direction: column; transition: transform 0.4s; }
        .menu-icon { width: 24px; height: 24px; position: relative; }
        .line { position: absolute; top: 50%; left: 0; width: 100%; height: 2px; background: currentColor; transition: 0.4s; }
        .v-line { transform: rotate(90deg); }

        .menu-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100vh; z-index: 90; pointer-events: none; transition: background 0.3s; }
        .menu-panel { position: absolute; top: 0; right: 0; width: 100%; max-width: 400px; height: 100%; background: #fff; padding: 8rem 3rem; display: flex; flex-direction: column; justify-content: space-between; transform: translateX(100%); transition: transform 0.6s cubic-bezier(0.7, 0, 0.3, 1); box-shadow: -10px 0 40px rgba(0,0,0,0.1); pointer-events: auto; }
        
        .menu-link { display: block; font-size: 1.8rem; font-weight: 700; color: #000; margin-bottom: 1.5rem; opacity: 0; transform: translateY(30px); transition: 0.5s; }
        .menu-link span { font-size: 0.9rem; color: var(--accent-color); margin-right: 15px; }
        .menu-link:hover { color: var(--accent-color); }
        .menu-socials { display: flex; gap: 20px; opacity: 0; transform: translateY(20px); transition: 0.5s; padding-top: 2rem; border-top: 1px solid #eee; }
        .menu-socials a { color: var(--text-secondary); font-weight: 500; }

        /* Ouverture Menu */
        body.menu-is-open { overflow: hidden; }
        body.menu-is-open .menu-text-inner { transform: translateY(-50%); }
        body.menu-is-open .line.v-line { transform: rotate(0deg); }
        body.menu-is-open .menu-icon { transform: rotate(180deg); }
        body.menu-is-open .menu-overlay { pointer-events: auto; background: rgba(0,0,0,0.2); }
        body.menu-is-open .menu-panel { transform: translateX(0); }
        body.menu-is-open .menu-link { opacity: 1; transform: translateY(0); }
        body.menu-is-open .menu-socials { opacity: 1; transform: translateY(0); }
        
        body.menu-is-open .menu-link:nth-child(1) { transition-delay: 0.1s; }
        body.menu-is-open .menu-link:nth-child(2) { transition-delay: 0.15s; }
        body.menu-is-open .menu-link:nth-child(3) { transition-delay: 0.2s; }
        body.menu-is-open .menu-link:nth-child(4) { transition-delay: 0.25s; }
        body.menu-is-open .menu-link:nth-child(5) { transition-delay: 0.3s; }
        body.menu-is-open .menu-socials { transition-delay: 0.4s; }

        /* Footer */
        .footer { padding: 3rem 0; margin-top: 5rem; background: #f8f9fa; border-top: 1px solid rgba(0,0,0,0.05); }
        .footer-content { display: flex; justify-content: space-between; }
        .social-links { display: flex; gap: 1rem; margin-top: 1rem; }
        .social-links a { width: 36px; height: 36px; border-radius: 50%; background: #fff; border: 1px solid #eee; display: flex; align-items: center; justify-content: center; color: #000; transition: 0.2s; }
        .social-links a:hover { background: #e5e5e5; }

        @media (max-width: 768px) {
            .footer-content { flex-direction: column; gap: 2rem; text-align: center; }
            .menu-panel { max-width: 100%; }
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>

    <header class="header">
        <div class="container-custom">
            <div class="header-content">
                <div class="logo-section">
                    <h1>DA BOIT Maxence</h1>
                    <p>Groupe TP: {{ groupe_tp|default('TP2') }}</p>
                </div>
                <button class="menu-toggle" id="menuToggle">
                    <div class="menu-text-wrap">
                        <span class="menu-text-inner"><span>Menu</span><span>Fermer</span></span>
                    </div>
                    <div class="menu-icon">
                        <span class="line"></span><span class="line v-line"></span>
                    </div>
                </button>
            </div>
        </div>
    </header>

    <div class="menu-overlay" id="menuOverlay">
        <div class="menu-panel">
            <nav class="menu-nav">
                <a href="{{ path('app_home') }}" class="menu-link"><span>01.</span> Accueil</a>
                <a href="{{ path('app_cv') }}" class="menu-link"><span>02.</span> Mon CV</a>
                <a href="{{ path('app_eportfolio') }}" class="menu-link"><span>03.</span> Portfolio</a>
                <a href="{{ path('app_passions') }}" class="menu-link"><span>04.</span> Mes Passions</a>
                <a href="{{ path('app_contact') }}" class="menu-link"><span>05.</span> Contact</a>
            </nav>
            <div class="menu-socials">
                <a href="#" target="_blank"><i class="fab fa-linkedin"></i> LinkedIn</a>
                <a href="#" target="_blank"><i class="fab fa-github"></i> GitHub</a>
                <a href="mailto:maxence.daboit@example.com"><i class="fas fa-envelope"></i> Email</a>
            </div>
        </div>
    </div>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer class="footer">
        <div class="container-custom">
            <div class="footer-content">
                <div class="footer-info">
                    <h3>DA BOIT Maxence</h3>
                    <p>Étudiant BUT R&T - IUT de Roanne</p>
                    <div class="social-links">
                        <a href="#"><i class="fab fa-linkedin"></i></a>
                        <a href="mailto:contact@example.com"><i class="fas fa-envelope"></i></a>
                        <a href="#"><i class="fab fa-github"></i></a>
                    </div>
                </div>
                <div class="copyright-notice">
                    <p><strong>© {{ "now"|date("Y") }} DA BOIT Maxence</strong></p>
                    <p>Tous droits réservés</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
    document.addEventListener('DOMContentLoaded', () => {
        // --- GESTION DU MENU UNIQUEMENT ---
        const toggleBtn = document.getElementById('menuToggle');
        const overlay = document.getElementById('menuOverlay');
        const toggleMenu = () => document.body.classList.toggle('menu-is-open');

        if(toggleBtn) toggleBtn.addEventListener('click', toggleMenu);
        if(overlay) overlay.addEventListener('click', (e) => { if(e.target === overlay) toggleMenu(); });
        document.querySelectorAll('.menu-link').forEach(l => l.addEventListener('click', () => document.body.classList.remove('menu-is-open')));
    });
    </script>

    {% block javascripts %}{% endblock %}
</body>
</html>