{% extends 'base.html.twig' %}

{% block title %}Accueil - DA BOIT Maxence{% endblock %}

{% block body %}
{# Hero Section avec informations personnelles - OBLIGATOIRE #}
<section class="bg-light py-5">
    <div class="container text-center">
        <h1 class="display-4 fw-bold">DA BOIT Maxence</h1>
        <p class="lead text-muted">Groupe TP: TP2</p>
        <p class="fs-5 mt-4">
            Étudiant passionné en Réseaux & Télécommunications à l'IUT de Roanne, 
            je développe mes compétences en administration réseau, connectivité 
            et développement d'applications pour les infrastructures IT.
        </p>
        <div class="university-logo mt-4">
            <img src="{{ asset('Image/Image IUT/IUT.ROANNE-LOGO-NOIR.png') }}" alt="IUT de Roanne" class="img-fluid">
        </div>
    </div>
</section>

{# Container "Pour en savoir plus" - OBLIGATOIRE cahier des charges #}
<section class="py-5">
    <div class="container">
        <div class="card border-primary shadow-lg">
            <div class="card-header bg-primary text-white">
                <h2 class="h4 mb-0">Pour en savoir plus</h2>
            </div>
            <div class="card-body">
                <p class="card-text">
                    Découvrez mes passions, mes loisirs et mes projets personnels. 
                    Cette section vous permettra de mieux me connaître au-delà de mon parcours académique et professionnel.
                </p>
                <a href="{{ path('app_passions') }}" class="btn btn-primary btn-lg">
                    <i class="fas fa-arrow-right me-2"></i>Découvrir mes passions
                </a>
            </div>
        </div>
    </div>
</section>

{# Tech Stack Défilant #}
<section class="bg-light">
    <div class="tech-stack-wrapper">
        <div class="tech-stack" id="techStack">
            {% for tech in ['cisco', 'c', 'python', 'symfony', 'html', 'css', 'ubuntu'] %}
            <div class="tech-item">
                <img src="{{ asset('Image/' ~ tech ~ '.png') }}" alt="{{ tech }}" width="50" height="50">
            </div>
            {% endfor %}
        </div>
    </div>
</section>

{# Aperçu Portfolio #}
<section class="py-5">
    <div class="container">
        <h2 class="display-6 fw-bold mb-4">Mes Projets</h2>
        <div class="row g-4">
            {% for project in projects|slice(0, 3) %}
            <div class="col-md-4">
                <a href="{{ path('app_project_detail', {id: project.id}) }}" class="text-decoration-none">
                    <div class="card h-100 shadow-sm border-0">
                        <div class="project-image {{ project.gradient }} rounded-top"></div>
                        <div class="card-body">
                            <h3 class="h5 fw-semibold">{{ project.title }}</h3>
                            <p class="text-muted small mb-0">{{ project.description }}</p>
                        </div>
                    </div>
                </a>
            </div>
            {% endfor %}
        </div>
        <div class="text-center mt-4">
            <a href="{{ path('app_eportfolio') }}" class="btn btn-outline-primary btn-lg">
                Voir tout mon E-Portfolio
            </a>
        </div>
    </div>
</section>
{% endblock %}

{% block javascripts %}
<script>
const stack = document.getElementById('techStack');
stack.innerHTML += stack.innerHTML + stack.innerHTML;
let scrollPos = 0;
function animate() {
    scrollPos += 1;
    if (scrollPos >= stack.scrollWidth / 3) scrollPos = 0;
    stack.style.transform = `translateX(-${scrollPos}px)`;
    requestAnimationFrame(animate);
}
animate();
</script>
{% endblock %}