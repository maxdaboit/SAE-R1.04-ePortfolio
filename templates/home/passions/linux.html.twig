{% extends 'base.html.twig' %}

{% block title %}Unix Ricing - DA BOIT Maxence{% endblock %}

{% block stylesheets %}
<style>
    /* --- 1. GRILLE BENTO (Nécessaire pour la mise en page) --- */
    .bento-grid {
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        gap: 1.5rem;
        padding-bottom: 4rem;
    }

    .bento-card {
        background: var(--card-bg); /* Utilise la variable dark définie plus bas */
        border-radius: 28px;
        padding: 2.5rem;
        border: 1px solid rgba(255,255,255,0.05); /* Bordure subtile en dark mode */
        overflow: hidden;
        position: relative;
    }

    .col-span-12 { grid-column: span 12; }
    .col-span-8 { grid-column: span 8; }
    .col-span-6 { grid-column: span 6; }
    .col-span-4 { grid-column: span 4; }

    @media (max-width: 968px) {
        .col-span-8, .col-span-6, .col-span-4 { grid-column: span 12; }
    }

    /* --- 2. THEME DARK SPÉCIFIQUE --- */
    :root {
        --bg-color: #0f0f12;         /* Fond très sombre */
        --card-bg: #1a1b26;          /* Fond des cartes (Tokyo Night) */
        --text-primary: #a9b1d6;     /* Texte gris clair */
        --text-secondary: #7aa2f7;   /* Bleu néon */
        --accent-term: #9ece6a;      /* Vert terminal */
    }

    /* Force le background sombre sur cette page */
    body { background-color: var(--bg-color) !important; color: var(--text-primary) !important; }
    
    h1, h2, h3 { color: #fff; }
    .badge { border: 1px solid var(--text-secondary); color: var(--text-secondary); background: transparent; }

    /* --- 3. STYLE TERMINAL --- */
    .terminal-window {
        background: #24283b;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        border: 1px solid #414868;
        font-family: 'Consolas', 'Monaco', monospace;
        width: 100%; height: 100%;
        display: flex; flex-direction: column;
        overflow: hidden;
    }
    .terminal-header {
        background: #1f2335; padding: 10px 15px;
        display: flex; align-items: center; gap: 8px;
    }
    .dot { width: 10px; height: 10px; border-radius: 50%; }
    .red { background: #f7768e; } .yellow { background: #e0af68; } .green { background: #9ece6a; }
    
    .terminal-body { padding: 1.5rem; font-size: 0.85rem; line-height: 1.6; color: #c0caf5; }
    .cmd { color: var(--accent-term); font-weight: bold; margin-right: 8px; }
    .path { color: #7aa2f7; font-weight: bold; }
    
    /* Animation curseur */
    .blink { animation: blinker 1s linear infinite; }
    @keyframes blinker { 50% { opacity: 0; } }
</style>
{% endblock %}

{% block body %}
<div class="container-custom py-5">
    
    <a href="{{ path('app_passions') }}" class="btn btn-sm btn-outline-light rounded-pill mb-4" style="border-color: #414868; color: #a9b1d6;">
        ← Retour
    </a>

    <div class="bento-grid">

        {# TITRE #}
        <div class="bento-card col-span-8 d-flex flex-column justify-content-center">
            <span class="badge mb-3" style="width: fit-content;">System Architecture</span>
            <h1 class="display-3 fw-bold mb-2">Unix <span style="color: var(--accent-term);">Ricing</span></h1>
            <p style="font-size: 1.1rem; opacity: 0.8;">
                L'art de l'efficacité : créer un environnement de travail où chaque pixel et chaque raccourci clavier est optimisé pour mes besoins.
            </p>
        </div>

        {# TERMINAL #}
        <div class="bento-card col-span-4 p-0" style="border:none; background: transparent;">
            <div class="terminal-window">
                <div class="terminal-header">
                    <div class="dot red"></div><div class="dot yellow"></div><div class="dot green"></div>
                </div>
                <div class="terminal-body">
                    <div><span class="cmd">user@arch</span><span class="path">~ $</span> neofetch</div>
                    <div class="mt-2 mb-3" style="opacity: 0.9;">
                        OS: Arch Linux x86_64<br>
                        Host: ROG Strix<br>
                        Kernel: 6.6.7-arch1-1<br>
                        WM: Hyprland (Wayland)<br>
                        Shell: zsh 5.9<br>
                        Term: Alacritty<br>
                    </div>
                    <div><span class="cmd">user@arch</span><span class="path">~ $</span> <span class="blink">_</span></div>
                </div>
            </div>
        </div>

        {# POURQUOI ARCH #}
        <div class="bento-card col-span-6">
            <h3>Pourquoi Arch Linux ?</h3>
            <p class="mt-3" style="opacity: 0.7;">
                Je ne cherche pas seulement à "utiliser" un ordinateur, je veux comprendre comment il fonctionne en profondeur.
            </p>
            <ul style="opacity: 0.7; padding-left: 1rem; margin-bottom: 0;">
                <li class="mb-2"><strong>Minimalisme :</strong> Installation de base uniquement (KISS principle), j'ajoute ce dont j'ai besoin.</li>
                <li class="mb-2"><strong>Workflow Clavier :</strong> Utilisation d'un Tiling Window Manager (Hyprland) pour ne jamais toucher la souris.</li>
                <li><strong>Automatisation :</strong> Scripts Bash/Python pour les tâches répétitives.</li>
            </ul>
        </div>

        {# DOTFILES #}
        <div class="bento-card col-span-6 d-flex flex-column justify-content-between">
            <div>
                <h3>Mes Dotfiles</h3>
                <p class="mt-2" style="opacity: 0.7;">
                    Mes fichiers de configuration sont versionnés sur GitHub. Ils définissent tout : des raccourcis clavier aux palettes de couleurs, en passant par la configuration de Neovim.
                </p>
            </div>
            <a href="https://github.com" target="_blank" class="btn btn-outline-light w-100 mt-3" style="border-color: #414868;">
                <i class="fab fa-github me-2"></i> Voir le repo sur GitHub
            </a>
        </div>

    </div>
</div>
{% endblock %}